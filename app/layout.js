import { Inter, Montserrat } from 'next/font/google';import './globals.css';const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });const montserrat = Montserrat({ subsets: ['latin'], variable: '--font-montserrat' });export const metadata = {  title: 'TezCodeStudios - Build Your Vision at Light Speed',  description: 'Gen-Z driven software agency delivering MVPs, AI tools, and custom digital products in days, not months.',};export default function RootLayout({ children }) {  return (    <html lang="en" className={`${inter.variable} ${montserrat.variable}`}>      <body className="bg-gray-950 text-white font-inter">{children}</body>    </html>  );}